---
phase: 06-sessionsperweek-enforcement-and-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements: [D-16]
must_haves:
  truths:
    - "xlsx package resolves from npm registry, not CDN tgz URL"
    - "npm install succeeds cleanly with the updated package.json"
    - "App.jsx import * as XLSX from 'xlsx' works with the npm package"
    - "Excel export still works after the package change"
  artifacts:
    - path: "package.json"
      provides: "xlsx as npm package dependency"
      contains: "\"xlsx\":"
  key_links:
    - from: "package.json"
      to: "src/App.jsx"
      via: "xlsx npm package resolves import * as XLSX from 'xlsx'"
      pattern: "\"xlsx\":"
---

<objective>
Migrate the xlsx dependency from the SheetJS CDN tgz URL to the standard npm registry package.
The CDN URL `https://cdn.sheetjs.com/xlsx-latest/xlsx-latest.tgz` is a reliability risk; the
npm package `xlsx` provides the same API and is the standard distribution channel.

Note: index.html already has no CDN script tag (verified). App.jsx already uses
`import * as XLSX from 'xlsx'` (correct). Only package.json needs updating.

Purpose: Reliability improvement — removes CDN availability dependency from the build.
Output: package.json with xlsx pointing to npm, node_modules updated.
</objective>

<execution_context>
@/c/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-sessionsperweek-enforcement-and-ui-polish/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update xlsx dependency to npm registry and reinstall</name>
  <files>package.json</files>
  <read_first>
    - package.json (verify the current xlsx entry and surrounding structure before editing)
  </read_first>
  <action>
In package.json, locate the `"xlsx"` entry in `"dependencies"`. It currently reads:
```json
"xlsx": "https://cdn.sheetjs.com/xlsx-latest/xlsx-latest.tgz"
```

Change it to:
```json
"xlsx": "^0.18.5"
```

Version `^0.18.5` is the stable community xlsx release on npm (same API surface as the SheetJS CDN version — `XLSX.utils.book_new()`, `XLSX.utils.aoa_to_sheet()`, `XLSX.utils.book_append_sheet()`, `XLSX.writeFile()` are all present). If a newer stable version is available on npm at execution time, use the latest `^0.18.x` version.

After editing package.json, run:
```bash
npm install
```

This regenerates node_modules with the npm package instead of the CDN tgz. Verify the install exits 0.

Do NOT change any other fields in package.json.
  </action>
  <verify>
    <automated>grep '"xlsx": "\^0' package.json && npm run build -- --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>
    - package.json xlsx entry points to npm version (^0.18.x), not CDN URL
    - `npm install` exits 0
    - `npm run build` exits 0 (xlsx import resolves correctly)
  </done>
  <acceptance_criteria>
    - grep '"xlsx":' package.json shows a semver value (^0.18.x), not the cdn.sheetjs.com URL
    - grep "cdn.sheetjs.com" package.json exits 1 (CDN URL is gone)
    - npm run build exits 0 (no unresolved import errors)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep '"xlsx":' package.json` — shows semver, not CDN URL
2. `grep "cdn.sheetjs.com" package.json` — exits 1 (URL removed)
3. `npm run build` — exits 0
4. `npm run lint` — exits 0
</verification>

<success_criteria>
xlsx is installed from npm registry. Build succeeds. No CDN URL reference in package.json.
</success_criteria>

<output>
After completion, create `.planning/phases/06-sessionsperweek-enforcement-and-ui-polish/06-02-SUMMARY.md`
</output>
