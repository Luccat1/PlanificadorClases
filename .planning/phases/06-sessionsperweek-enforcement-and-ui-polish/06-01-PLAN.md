---
phase: 06-sessionsperweek-enforcement-and-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useCourseData.js
  - src/logic/scheduleEngine.js
autonomous: true
requirements: [D-02, D-12]
must_haves:
  truths:
    - "sessionsPerWeek defaults to 0 (no cap) for new and existing users"
    - "perDayHours map is available in courseData with empty-object default"
    - "Engine produces sessions with per-day hours when perDayHours map is non-empty"
    - "Engine falls back to hoursPerSession for days absent from perDayHours map"
    - "Backwards-compat merge pattern keeps old localStorage data working"
  artifacts:
    - path: "src/hooks/useCourseData.js"
      provides: "INITIAL_COURSE_DATA with sessionsPerWeek=0 and perDayHours={}"
      contains: "sessionsPerWeek: 0"
    - path: "src/logic/scheduleEngine.js"
      provides: "perDayHours branch in calculateSchedule"
      contains: "perDayHours"
  key_links:
    - from: "src/hooks/useCourseData.js"
      to: "INITIAL_COURSE_DATA"
      via: "sessionsPerWeek: 0 default"
      pattern: "sessionsPerWeek: 0"
    - from: "src/logic/scheduleEngine.js"
      to: "courseData.perDayHours"
      via: "per-day hours branch"
      pattern: "perDayHours"
---

<objective>
Update the data model and schedule engine to support two new Phase 6 features:
(1) sessionsPerWeek default changes from 2 to 0 (no cap), and
(2) perDayHours map enables variable class duration per weekday.

Purpose: These are the foundational data changes that all UI plans in Wave 2 depend on.
Output: useCourseData.js with updated INITIAL_COURSE_DATA; scheduleEngine.js with perDayHours branch.
</objective>

<execution_context>
@/c/Users/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sessionsperweek-enforcement-and-ui-polish/06-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update INITIAL_COURSE_DATA defaults in useCourseData.js</name>
  <files>src/hooks/useCourseData.js</files>
  <read_first>
    - src/hooks/useCourseData.js (current INITIAL_COURSE_DATA shape and merge pattern)
  </read_first>
  <behavior>
    - sessionsPerWeek field value after change: 0 (not 2)
    - perDayHours field must be present with default value {}
    - localStorage merge pattern { ...INITIAL_COURSE_DATA, ...JSON.parse(saved) } ensures old saved blobs without perDayHours get {} as default
    - Old saved blobs with sessionsPerWeek: 2 will keep their value (user-saved preference)
    - Old saved blobs without sessionsPerWeek will get 0 from INITIAL_COURSE_DATA spread
  </behavior>
  <action>
In src/hooks/useCourseData.js, make exactly two changes to INITIAL_COURSE_DATA:

1. Change line `sessionsPerWeek: 2,` to `sessionsPerWeek: 0,`

2. Add `perDayHours: {},` as a new field — place it immediately after `hoursPerSession: 2,` so it reads:
```
    hoursPerSession: 2,
    perDayHours: {},
```

No other changes needed. The existing localStorage merge pattern `{ ...INITIAL_COURSE_DATA, ...JSON.parse(saved) }` already provides backwards-compat: old blobs without `perDayHours` will receive `{}` as default.

Do NOT change the merge initializer, the reset function, or any handlers — they are all already correct.
  </action>
  <verify>
    <automated>grep -n "sessionsPerWeek: 0" src/hooks/useCourseData.js && grep -n "perDayHours: {}" src/hooks/useCourseData.js</automated>
  </verify>
  <done>
    - `sessionsPerWeek: 0` appears in INITIAL_COURSE_DATA
    - `perDayHours: {}` appears in INITIAL_COURSE_DATA
    - `npm test` passes (no regressions)
  </done>
  <acceptance_criteria>
    - grep "sessionsPerWeek: 0" src/hooks/useCourseData.js exits 0
    - grep "perDayHours: {}" src/hooks/useCourseData.js exits 0
    - grep "sessionsPerWeek: 2" src/hooks/useCourseData.js exits 1 (the old default is gone)
    - npm test exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add perDayHours branch to calculateSchedule in scheduleEngine.js</name>
  <files>src/logic/scheduleEngine.js</files>
  <read_first>
    - src/logic/scheduleEngine.js (full file — understand effNormal/effRecovery derivation and session push logic)
    - src/logic/utils.js (getEffectiveHours signature)
    - src/logic/constants.js (DAY_MAPPING keys — used to resolve dayKey per session)
  </read_first>
  <behavior>
    - When courseData.perDayHours is empty {} or undefined: engine behavior is identical to before (uses hoursPerSession for all sessions)
    - When courseData.perDayHours is { monday: 1.5, thursday: 1.0 }: session on Monday uses 1.5h, session on Thursday uses 1.0h
    - Per-day effective hours: getEffectiveHours(perDayHours[dayKey], courseData.hourType) when dayKey is present in map
    - Recovery modifier still applies per-day: if session is recovery AND has a per-day override, the recovery hours = getEffectiveHours(perDayHours[dayKey] + recoveryBonusHours, courseData.hourType)
    - Fallback: if dayKey is not in perDayHours map, use effNormal (and effRecovery for recovery sessions) as before
    - chronoHours on each session object reflects the per-day value when applicable
  </behavior>
  <action>
In src/logic/scheduleEngine.js, modify the calculateSchedule function. The existing code computes `effNormal` and `effRecovery` once at the top. Add per-session resolution inside the while loop, after the sessionsPerWeek cap check and `weekSessionCounts.set(...)` call.

**Step 1:** After `weekSessionCounts.set(weekKey, weekCount + 1);`, compute the dayKey for the current date. This already happens later in the code (it's used for `sessions.push()`). Move or duplicate the dayKey resolution to before the session push, and use it for per-day hours:

```javascript
const dayKey =
    Object.keys(DAY_MAPPING).find(k => DAY_MAPPING[k] === dayNum) || 'monday';
```

**Step 2:** Resolve per-day hours. Insert this block immediately after the dayKey line:

```javascript
// Per-day variable duration (D-12)
const perDayHours = courseData.perDayHours || {};
const hasPerDay = Object.keys(perDayHours).length > 0 && perDayHours[dayKey] != null;
const baseHours = hasPerDay ? perDayHours[dayKey] : courseData.hoursPerSession;
const effDay = getEffectiveHours(baseHours, courseData.hourType);
const effDayRecovery = getEffectiveHours(baseHours + recoveryBonusHours, courseData.hourType);
```

**Step 3:** Replace the session `currentEff` line:
- Old: `const currentEff = isRecovery ? effRecovery : effNormal;`
- New: `const currentEff = isRecovery ? effDayRecovery : effDay;`

**Step 4:** In the `sessions.push({...})` object, update `chronoHours` to use `baseHours`:
- Old: `chronoHours: isRecovery ? courseData.hoursPerSession + recoveryBonusHours : courseData.hoursPerSession,`
- New: `chronoHours: isRecovery ? baseHours + recoveryBonusHours : baseHours,`

**Step 5:** Remove the now-duplicate `const dayKey = ...` line that appears in the push (since dayKey is now declared before). Ensure `dayKey` is only declared once in the loop body.

**Important:** The `effNormal` and `effRecovery` variables computed at the top of the function body are now used only as fallbacks via the `hasPerDay` ternary. Keep them — they remain the correct values when perDayHours is empty. The `effDay` variable will equal `effNormal` when `hasPerDay` is false.

Do NOT change the function signature, imports, exports, or the outer guard clauses.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    - grep "perDayHours" src/logic/scheduleEngine.js shows the new branch
    - `npm test` passes (existing tests unaffected because perDayHours defaults to {} → hasPerDay is false → effDay === effNormal)
  </done>
  <acceptance_criteria>
    - grep -c "perDayHours" src/logic/scheduleEngine.js returns 3 or more matches
    - grep "hasPerDay" src/logic/scheduleEngine.js exits 0
    - grep "effDay" src/logic/scheduleEngine.js exits 0
    - npm test exits 0
    - The dayKey variable is declared exactly once in the while loop body (no duplicate const declarations)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "sessionsPerWeek: 0" src/hooks/useCourseData.js` — passes
2. `grep "perDayHours: {}" src/hooks/useCourseData.js` — passes
3. `grep "perDayHours" src/logic/scheduleEngine.js` — passes (3+ matches)
4. `npm test` — all existing tests pass
5. No TypeScript/ESLint errors: `npm run lint` exits 0
</verification>

<success_criteria>
- sessionsPerWeek default is 0 in INITIAL_COURSE_DATA
- perDayHours: {} is in INITIAL_COURSE_DATA
- calculateSchedule resolves per-day effective hours when perDayHours map has entries for the session's dayKey
- All existing test suites pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/06-sessionsperweek-enforcement-and-ui-polish/06-01-SUMMARY.md`
</output>
